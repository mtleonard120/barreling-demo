import React from 'react';

// packages

// components

// utils

// styles

// types
interface IButtonProps {

}

// helpers

// primary component
export const Button: React.SFC<IButtonProps> = (props) => {

    return (
        <button></button>
    );
};
