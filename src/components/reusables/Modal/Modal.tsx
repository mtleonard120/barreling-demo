import React from 'react';

// packages

// components

// utils

// styles

// types
interface IModalProps {

}

// helpers

// primary component
export const Modal: React.SFC<IModalProps> = (props) => {
    return (
        <div></div>
    );
};
